<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="550" onload="init(evt)" viewBox="0 0 1200 550" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="550" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="533.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="533.00"> </text><svg id="frames" x="10" width="1180" total_samples="68508"><g><title>cellular-automaton.exe`alloc::raw_vec::RawVec&lt;u8,alloc::alloc::Global&gt;::capacity (568 samples, 0.83%)</title><rect x="27.1122%" y="197" width="0.8291%" height="15" fill="rgb(227,0,7)" fg:x="18574" fg:w="568"/><text x="27.3622%" y="207.50"></text></g><g><title>cellular-automaton.exe`core::mem::size_of (535 samples, 0.78%)</title><rect x="27.1603%" y="181" width="0.7809%" height="15" fill="rgb(217,0,24)" fg:x="18607" fg:w="535"/><text x="27.4103%" y="191.50"></text></g><g><title>cellular-automaton.exe`core::num::niche_types::UsizeNoHighBit::as_inner (117 samples, 0.17%)</title><rect x="27.7705%" y="165" width="0.1708%" height="15" fill="rgb(221,193,54)" fg:x="19025" fg:w="117"/><text x="28.0205%" y="175.50"></text></g><g><title>cellular-automaton.exe`alloc::vec::Vec&lt;u8,alloc::alloc::Global&gt;::as_mut_ptr (1,022 samples, 1.49%)</title><rect x="27.9413%" y="197" width="1.4918%" height="15" fill="rgb(248,212,6)" fg:x="19142" fg:w="1022"/><text x="28.1913%" y="207.50"></text></g><g><title>cellular-automaton.exe`alloc::raw_vec::RawVec&lt;u8,alloc::alloc::Global&gt;::ptr (573 samples, 0.84%)</title><rect x="28.5967%" y="181" width="0.8364%" height="15" fill="rgb(208,68,35)" fg:x="19591" fg:w="573"/><text x="28.8467%" y="191.50"></text></g><g><title>cellular-automaton.exe`alloc::raw_vec::RawVecInner&lt;alloc::alloc::Global&gt;::ptr (573 samples, 0.84%)</title><rect x="28.5967%" y="165" width="0.8364%" height="15" fill="rgb(232,128,0)" fg:x="19591" fg:w="573"/><text x="28.8467%" y="175.50"></text></g><g><title>cellular-automaton.exe`alloc::raw_vec::RawVecInner&lt;alloc::alloc::Global&gt;::non_null (573 samples, 0.84%)</title><rect x="28.5967%" y="149" width="0.8364%" height="15" fill="rgb(207,160,47)" fg:x="19591" fg:w="573"/><text x="28.8467%" y="159.50"></text></g><g><title>cellular-automaton.exe`alloc::vec::Vec&lt;u8,alloc::alloc::Global&gt;::push&lt;u8,alloc::alloc::Global&gt; (7,279 samples, 10.63%)</title><rect x="18.8547%" y="213" width="10.6250%" height="15" fill="rgb(228,23,34)" fg:x="12917" fg:w="7279"/><text x="19.1047%" y="223.50">cellular-automat..</text></g><g><title>cellular-automaton.exe`core::ptr::write (32 samples, 0.05%)</title><rect x="29.4331%" y="197" width="0.0467%" height="15" fill="rgb(218,30,26)" fg:x="20164" fg:w="32"/><text x="29.6831%" y="207.50"></text></g><g><title>cellular-automaton.exe`alloc::raw_vec::RawVec&lt;u8,alloc::alloc::Global&gt;::ptr (1,519 samples, 2.22%)</title><rect x="36.9388%" y="149" width="2.2173%" height="15" fill="rgb(220,122,19)" fg:x="25306" fg:w="1519"/><text x="37.1888%" y="159.50">c..</text></g><g><title>cellular-automaton.exe`alloc::raw_vec::RawVecInner&lt;alloc::alloc::Global&gt;::ptr (1,519 samples, 2.22%)</title><rect x="36.9388%" y="133" width="2.2173%" height="15" fill="rgb(250,228,42)" fg:x="25306" fg:w="1519"/><text x="37.1888%" y="143.50">c..</text></g><g><title>cellular-automaton.exe`alloc::raw_vec::RawVecInner&lt;alloc::alloc::Global&gt;::non_null (1,519 samples, 2.22%)</title><rect x="36.9388%" y="117" width="2.2173%" height="15" fill="rgb(240,193,28)" fg:x="25306" fg:w="1519"/><text x="37.1888%" y="127.50">c..</text></g><g><title>cellular-automaton.exe`core::ub_checks::is_valid_allocation_size (20,757 samples, 30.30%)</title><rect x="50.9634%" y="101" width="30.2987%" height="15" fill="rgb(216,20,37)" fg:x="34914" fg:w="20757"/><text x="51.2134%" y="111.50">cellular-automaton.exe`core::ub_checks::is_valid_..</text></g><g><title>cellular-automaton.exe`core::fmt::Arguments::new_const(ref$ (597 samples, 0.87%)</title><rect x="86.8745%" y="53" width="0.8714%" height="15" fill="rgb(206,188,39)" fg:x="59516" fg:w="597"/><text x="87.1245%" y="63.50"></text></g><g><title>cellular-automaton.exe`core::num::impl$11::is_power_of_two (4,226 samples, 6.17%)</title><rect x="87.7460%" y="53" width="6.1686%" height="15" fill="rgb(217,207,13)" fg:x="60113" fg:w="4226"/><text x="87.9960%" y="63.50">cellular..</text></g><g><title>cellular-automaton.exe`core::num::impl$11::count_ones (4,226 samples, 6.17%)</title><rect x="87.7460%" y="37" width="6.1686%" height="15" fill="rgb(231,73,38)" fg:x="60113" fg:w="4226"/><text x="87.9960%" y="47.50">cellular..</text></g><g><title>cellular-automaton.exe`alloc::vec::impl$8::deref(alloc::vec::Vec (42,903 samples, 62.62%)</title><rect x="32.0240%" y="197" width="62.6248%" height="15" fill="rgb(225,20,46)" fg:x="21939" fg:w="42903"/><text x="32.2740%" y="207.50">cellular-automaton.exe`alloc::vec::impl$8::deref(alloc::vec::Vec</text></g><g><title>cellular-automaton.exe`alloc::vec::Vec&lt;u8,alloc::alloc::Global&gt;::as_slice (42,903 samples, 62.62%)</title><rect x="32.0240%" y="181" width="62.6248%" height="15" fill="rgb(210,31,41)" fg:x="21939" fg:w="42903"/><text x="32.2740%" y="191.50">cellular-automaton.exe`alloc::vec::Vec&lt;u8,alloc::alloc::Global&gt;::as_slice</text></g><g><title>cellular-automaton.exe`alloc::vec::Vec&lt;u8,alloc::alloc::Global&gt;::as_ptr (41,579 samples, 60.69%)</title><rect x="33.9566%" y="165" width="60.6922%" height="15" fill="rgb(221,200,47)" fg:x="23263" fg:w="41579"/><text x="34.2066%" y="175.50">cellular-automaton.exe`alloc::vec::Vec&lt;u8,alloc::alloc::Global&gt;::as_ptr</text></g><g><title>cellular-automaton.exe`core::ptr::slice_from_raw_parts (38,017 samples, 55.49%)</title><rect x="39.1560%" y="149" width="55.4928%" height="15" fill="rgb(226,26,5)" fg:x="26825" fg:w="38017"/><text x="39.4060%" y="159.50">cellular-automaton.exe`core::ptr::slice_from_raw_parts</text></g><g><title>cellular-automaton.exe`core::ptr::metadata::from_raw_parts (38,017 samples, 55.49%)</title><rect x="39.1560%" y="133" width="55.4928%" height="15" fill="rgb(249,33,26)" fg:x="26825" fg:w="38017"/><text x="39.4060%" y="143.50">cellular-automaton.exe`core::ptr::metadata::from_raw_parts</text></g><g><title>cellular-automaton.exe`core::slice::raw::from_raw_parts::precondition_check(tuple$ (36,258 samples, 52.93%)</title><rect x="41.7236%" y="117" width="52.9252%" height="15" fill="rgb(235,183,28)" fg:x="28584" fg:w="36258"/><text x="41.9736%" y="127.50">cellular-automaton.exe`core::slice::raw::from_raw_parts::precondition_check(tuple$</text></g><g><title>cellular-automaton.exe`core::ub_checks::maybe_is_aligned_and_not_null(tuple$ (9,171 samples, 13.39%)</title><rect x="81.2620%" y="101" width="13.3868%" height="15" fill="rgb(221,5,38)" fg:x="55671" fg:w="9171"/><text x="81.5120%" y="111.50">cellular-automaton.e..</text></g><g><title>cellular-automaton.exe`core::ub_checks::maybe_is_aligned_and_not_null::runtime(tuple$ (8,695 samples, 12.69%)</title><rect x="81.9569%" y="85" width="12.6919%" height="15" fill="rgb(247,18,42)" fg:x="56147" fg:w="8695"/><text x="82.2069%" y="95.50">cellular-automaton...</text></g><g><title>cellular-automaton.exe`core::ptr::const_ptr::impl$0::is_aligned_to(tuple$ (7,486 samples, 10.93%)</title><rect x="83.7216%" y="69" width="10.9272%" height="15" fill="rgb(241,131,45)" fg:x="57356" fg:w="7486"/><text x="83.9716%" y="79.50">cellular-automat..</text></g><g><title>cellular-automaton.exe`core::ptr::const_ptr::impl$0::is_null::runtime (503 samples, 0.73%)</title><rect x="93.9146%" y="53" width="0.7342%" height="15" fill="rgb(249,31,29)" fg:x="64339" fg:w="503"/><text x="94.1646%" y="63.50"></text></g><g><title>cellular-automaton.exe`alloc::vec::impl$13::index&lt;u8,usize,alloc::alloc::Global&gt;(alloc::vec::Vec (48,254 samples, 70.44%)</title><rect x="29.4798%" y="213" width="70.4356%" height="15" fill="rgb(225,111,53)" fg:x="20196" fg:w="48254"/><text x="29.7298%" y="223.50">cellular-automaton.exe`alloc::vec::impl$13::index&lt;u8,usize,alloc::alloc::Global&gt;(alloc::vec::Vec</text></g><g><title>cellular-automaton.exe`core::slice::index::impl$2::index&lt;u8&gt;(unsigned long long, ref$&lt;slice2$ (3,608 samples, 5.27%)</title><rect x="94.6488%" y="197" width="5.2665%" height="15" fill="rgb(238,160,17)" fg:x="64842" fg:w="3608"/><text x="94.8988%" y="207.50">cellul..</text></g><g><title>cellular-automaton.exe`cellular_automaton::diagonal::Diagonal::count_state_in_pattern (7 samples, 0.01%)</title><rect x="99.9241%" y="197" width="0.0102%" height="15" fill="rgb(214,148,48)" fg:x="68456" fg:w="7"/><text x="100.1741%" y="207.50"></text></g><g><title>cellular-automaton.exe`cellular_automaton::diagonal::Fast30::is_doubling (19 samples, 0.03%)</title><rect x="99.9226%" y="213" width="0.0277%" height="15" fill="rgb(232,36,49)" fg:x="68455" fg:w="19"/><text x="100.1726%" y="223.50"></text></g><g><title>cellular-automaton.exe`cellular_automaton::diagonal::Diagonal::has_state_in_pattern (11 samples, 0.02%)</title><rect x="99.9343%" y="197" width="0.0161%" height="15" fill="rgb(209,103,24)" fg:x="68463" fg:w="11"/><text x="100.1843%" y="207.50"></text></g><g><title>cellular-automaton.exe`core::slice::impl$0::contains&lt;u8&gt;(ref$&lt;slice2$ (8 samples, 0.01%)</title><rect x="99.9387%" y="181" width="0.0117%" height="15" fill="rgb(229,88,8)" fg:x="68466" fg:w="8"/><text x="100.1887%" y="191.50"></text></g><g><title>cellular-automaton.exe`cellular_automaton::diagonal::Fast30::next (68,453 samples, 99.92%)</title><rect x="0.0321%" y="229" width="99.9197%" height="15" fill="rgb(213,181,19)" fg:x="22" fg:w="68453"/><text x="0.2821%" y="239.50">cellular-automaton.exe`cellular_automaton::diagonal::Fast30::next</text></g><g><title>cellular-automaton.exe`cellular_automaton::diagonal::Fast30::evolve (68,460 samples, 99.93%)</title><rect x="0.0277%" y="245" width="99.9299%" height="15" fill="rgb(254,191,54)" fg:x="19" fg:w="68460"/><text x="0.2777%" y="255.50">cellular-automaton.exe`cellular_automaton::diagonal::Fast30::evolve</text></g><g><title>`0x7FFB9E59C5DC (68,476 samples, 99.95%)</title><rect x="0.0058%" y="485" width="99.9533%" height="15" fill="rgb(241,83,37)" fg:x="4" fg:w="68476"/><text x="0.2558%" y="495.50">`0x7FFB9E59C5DC</text></g><g><title>`0x7FFB9D38E8D7 (68,476 samples, 99.95%)</title><rect x="0.0058%" y="469" width="99.9533%" height="15" fill="rgb(233,36,39)" fg:x="4" fg:w="68476"/><text x="0.2558%" y="479.50">`0x7FFB9D38E8D7</text></g><g><title>cellular-automaton.exe`__scrt_common_main_seh() (68,470 samples, 99.94%)</title><rect x="0.0146%" y="453" width="99.9445%" height="15" fill="rgb(226,3,54)" fg:x="10" fg:w="68470"/><text x="0.2646%" y="463.50">cellular-automaton.exe`__scrt_common_main_seh()</text></g><g><title>cellular-automaton.exe`invoke_main() (68,462 samples, 99.93%)</title><rect x="0.0263%" y="437" width="99.9329%" height="15" fill="rgb(245,192,40)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="447.50">cellular-automaton.exe`invoke_main()</text></g><g><title>cellular-automaton.exe`main (68,462 samples, 99.93%)</title><rect x="0.0263%" y="421" width="99.9329%" height="15" fill="rgb(238,167,29)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="431.50">cellular-automaton.exe`main</text></g><g><title>cellular-automaton.exe`std::rt::lang_start&lt;tuple$&lt;&gt; &gt;(void (*) (68,462 samples, 99.93%)</title><rect x="0.0263%" y="405" width="99.9329%" height="15" fill="rgb(232,182,51)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="415.50">cellular-automaton.exe`std::rt::lang_start&lt;tuple$&lt;&gt; &gt;(void (*)</text></g><g><title>cellular-automaton.exe`std::rt::lang_start_internal (68,462 samples, 99.93%)</title><rect x="0.0263%" y="389" width="99.9329%" height="15" fill="rgb(231,60,39)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="399.50">cellular-automaton.exe`std::rt::lang_start_internal</text></g><g><title>cellular-automaton.exe`std::panic::catch_unwind (68,462 samples, 99.93%)</title><rect x="0.0263%" y="373" width="99.9329%" height="15" fill="rgb(208,69,12)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="383.50">cellular-automaton.exe`std::panic::catch_unwind</text></g><g><title>cellular-automaton.exe`std::panicking::try (68,462 samples, 99.93%)</title><rect x="0.0263%" y="357" width="99.9329%" height="15" fill="rgb(235,93,37)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="367.50">cellular-automaton.exe`std::panicking::try</text></g><g><title>cellular-automaton.exe`std::panicking::try::do_call (68,462 samples, 99.93%)</title><rect x="0.0263%" y="341" width="99.9329%" height="15" fill="rgb(213,116,39)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="351.50">cellular-automaton.exe`std::panicking::try::do_call</text></g><g><title>cellular-automaton.exe`std::rt::lang_start_internal::closure$0 (68,462 samples, 99.93%)</title><rect x="0.0263%" y="325" width="99.9329%" height="15" fill="rgb(222,207,29)" fg:x="18" fg:w="68462"/><text x="0.2763%" y="335.50">cellular-automaton.exe`std::rt::lang_start_internal::closure$0</text></g><g><title>cellular-automaton.exe`std::rt::lang_start::closure$0&lt;tuple$&lt;&gt; &gt;(std::rt::lang_start::closure_env$0&lt;tuple$ (68,461 samples, 99.93%)</title><rect x="0.0277%" y="309" width="99.9314%" height="15" fill="rgb(206,96,30)" fg:x="19" fg:w="68461"/><text x="0.2777%" y="319.50">cellular-automaton.exe`std::rt::lang_start::closure$0&lt;tuple$&lt;&gt; &gt;(std::rt::lang_start::closure_env$0&lt;tuple$</text></g><g><title>cellular-automaton.exe`std::sys::backtrace::__rust_begin_short_backtrace&lt;void (*)(),tuple$&lt;&gt; &gt;(void (*) (68,461 samples, 99.93%)</title><rect x="0.0277%" y="293" width="99.9314%" height="15" fill="rgb(218,138,4)" fg:x="19" fg:w="68461"/><text x="0.2777%" y="303.50">cellular-automaton.exe`std::sys::backtrace::__rust_begin_short_backtrace&lt;void (*)(),tuple$&lt;&gt; &gt;(void (*)</text></g><g><title>cellular-automaton.exe`core::ops::function::FnOnce::call_once&lt;void (*)(),tuple$&lt;&gt; &gt;(void (*) (68,461 samples, 99.93%)</title><rect x="0.0277%" y="277" width="99.9314%" height="15" fill="rgb(250,191,14)" fg:x="19" fg:w="68461"/><text x="0.2777%" y="287.50">cellular-automaton.exe`core::ops::function::FnOnce::call_once&lt;void (*)(),tuple$&lt;&gt; &gt;(void (*)</text></g><g><title>cellular-automaton.exe`cellular_automaton::main (68,461 samples, 99.93%)</title><rect x="0.0277%" y="261" width="99.9314%" height="15" fill="rgb(239,60,40)" fg:x="19" fg:w="68461"/><text x="0.2777%" y="271.50">cellular-automaton.exe`cellular_automaton::main</text></g><g><title>`0x7FFB9E5D8984 (11 samples, 0.02%)</title><rect x="99.9664%" y="421" width="0.0161%" height="15" fill="rgb(206,27,48)" fg:x="68485" fg:w="11"/><text x="100.2164%" y="431.50"></text></g><g><title>`0x7FFB9E554560 (11 samples, 0.02%)</title><rect x="99.9664%" y="405" width="0.0161%" height="15" fill="rgb(225,35,8)" fg:x="68485" fg:w="11"/><text x="100.2164%" y="415.50"></text></g><g><title>`0x7FFB9E50FAD0 (11 samples, 0.02%)</title><rect x="99.9664%" y="389" width="0.0161%" height="15" fill="rgb(250,213,24)" fg:x="68485" fg:w="11"/><text x="100.2164%" y="399.50"></text></g><g><title>`0x7FFB9E56BA79 (13 samples, 0.02%)</title><rect x="99.9664%" y="437" width="0.0190%" height="15" fill="rgb(247,123,22)" fg:x="68485" fg:w="13"/><text x="100.2164%" y="447.50"></text></g><g><title>all (68,508 samples, 100%)</title><rect x="0.0000%" y="501" width="100.0000%" height="15" fill="rgb(231,138,38)" fg:x="0" fg:w="68508"/><text x="0.2500%" y="511.50"></text></g><g><title>`0x7FFB9E5F270E (28 samples, 0.04%)</title><rect x="99.9591%" y="485" width="0.0409%" height="15" fill="rgb(231,145,46)" fg:x="68480" fg:w="28"/><text x="100.2091%" y="495.50"></text></g><g><title>`0x7FFB9E56AA22 (28 samples, 0.04%)</title><rect x="99.9591%" y="469" width="0.0409%" height="15" fill="rgb(251,118,11)" fg:x="68480" fg:w="28"/><text x="100.2091%" y="479.50"></text></g><g><title>`0x7FFB9E56AC38 (28 samples, 0.04%)</title><rect x="99.9591%" y="453" width="0.0409%" height="15" fill="rgb(217,147,25)" fg:x="68480" fg:w="28"/><text x="100.2091%" y="463.50"></text></g></svg></svg>